import React from 'react';
import { FaFilter, FaSearch } from 'react-icons/fa';

const DemandsFilter = ({ onOpenFilterModal, searchTerm, onSearchChange, activeFiltersCount }) => {
  return (
   <></>
  );
};

export default DemandsFilter;
